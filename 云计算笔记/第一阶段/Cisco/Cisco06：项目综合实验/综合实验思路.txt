1，所有交换机创建vlan1、vlan2
2，配置服务器ip，并将服务器所连接的交换机接口加入相应vlan
3，所有交换机之间创建以太通道，并配置中继链路
4，配置三层交换机SVI地址
5，配置hsrp，使MS1成为vlan1的活跃路由器，vlan2的备份路由器。
使MS2成为vlan1的备份路由器，vlan2的活跃路由器。
6，配置stp，使MS1成为vlan1的主根，vlan2的次根。
使MS2成为vlan1的次根，vlan2的主根。
7，配置所有三层设备的ip与动态路由，使内网互通
8，配置静态nat使内网服务器可以访问外网，使用外网pc可以访问内网服务

